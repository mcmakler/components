mixin table(rows)
    table
        thead
            each row, index in rows
                if (index === 0)
                    tr
                        each cell in row
                            th #{cell}
        tbody
            each row, index in rows
                if (index > 0)
                    tr
                        each cell in row
                            td #{cell}

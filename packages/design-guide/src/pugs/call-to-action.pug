mixin call-to-action(label, link, target)
    if target
        a.button.button.button_large.button_primary(href=""+link target=""+target) #{label}
    else
        a.button.button.button_large.button_primary(href=""+link) #{label}
